{
  "name": "blog-reader",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "tsc --noEmit && markdownlint . --fix && prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^25.0.1",
    "marked": "^16.2.1",
    "prettier": "^3.5.3",
    "typescript": "^5.3.3",
    "vite": "^6.1.6"
  },
  "dependencies": {
    "highlight.js": "^11.11.1",
    "marked-highlight": "^2.2.3"
  }
}
